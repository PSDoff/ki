extends ../layout
block head_content
block body_content
    main(class=`single-tap`)
        section(class=`tap-${tap.key} flex` data-volume-original=`${tap.initialVolume}` data-volume-remaining=`${tap.volume}` data-volume-percent=`${percentageRemaining}`)
            include ../snippets/tap-layout
